var assert = require('chai').assert;
//var test = require("../lib/queries");
//var test = require("../tmp/test");

function f2 (a, callback){
       callback(a);
      }

describe('firstFunc', function() {
  
 describe('name is ', function () {
     
    it('should be', function (done) {
       f2(19, function(arg){
           
       });
      //assert.equal("abc", "abc");
      done();
    });
    
  });
  
}) 

/*
if (a > 10) {
           throw new Error("случилась какая-то неведомая хрень!");
       }
       */