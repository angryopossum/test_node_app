extends layout

block content
  h1= title
  p Welcome to #{title}
  
  .leftside
   - for (var i =0; i < Object.keys(brands).length; i++) {
   | #{brands[i]._id}
   br
   - }
   
  .main 
  | Products User Category